#!/bin/bash
# Begin LSF Directives
#BSUB -P GEO127
#BSUB -W 0:10
#BSUB -nnodes 2
#BSUB -alloc_flags gpumps
#BSUB -J bench
#BSUB -o bench.%J
#BSUB -e bench.%J

export CH_OUTPUT_INTERVAL=1
export CH_TIMER=1
module load cuda
module load hdf5
date
jsrun -n 2  -r 1 -a 1 -g 1 ../../../exec/main.exe ./inputs
